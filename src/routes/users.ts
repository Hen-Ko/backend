import { Router } from 'express';
export default async function users(router: Router) {
    // eslint-disable-next-line @typescript-eslint/no-unused-vars
    router.get('/users/:id', async (req, res) => {
        // soon
    });
}
